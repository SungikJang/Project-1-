using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class Movements : MonoBehaviour
{
    GameObject Player = null;
    public static void Jump()
    {
        if(PlayerController.CharecterState == Constant.State.Jumping)
        {
            return;
        }
        if (PlayerController.CharecterState == Constant.State.Idle)
        {
            Animator Movement = GetComponent<Animator>();
            Movement.Play("Jump");
        }
        if (PlayerController.CharecterState == Constant.State.Move)
        {
            PlayerController.CharecterState = Constant.State.Idle;
            Animator Movement = GetComponent<Animator>();
            Movement.Play("Jump");
            PlayerController.CharecterState = Constant.State.Move;
        }
    }
}*/
